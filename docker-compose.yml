services:
  web:
    build: .
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
    networks:
      proxy-net:
        aliases:
          - digest-web
    restart: unless-stopped

networks:
  proxy-net:
    external: true
